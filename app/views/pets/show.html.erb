<h1><%= @pet.name %></h1>
<p><%= @pet.species %></p>
<p>Found <%= @pet.found_days_ago %></p>

<%= link_to 'Edit pet', edit_pets_path(@pet)  %>
<%= link_to 'Pet has been returned to owner', pet_path(@pet), method: :delete, data: { confirm: 'Are you sure pet has the right owner?'} %>

<%= link_to 'Back to lost pets', pets_path  %>
